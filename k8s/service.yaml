apiVersion: v1
kind: Service

metadata:
  name: kubernetes-web-app-service

spec:
  type: NodePort

  selector:
    app: kubernetes-web-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80